// This check is done here before any plugins that may require java 17 are able to load.
if (!JavaVersion.current().isCompatibleWith(JavaVersion.VERSION_17)) {
	throw new UnsupportedOperationException("InfdevMappings's buildscript requires Java 17 or higher.")
}

rootProject.name = 'InfdevMappings'
